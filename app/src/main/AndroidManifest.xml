<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">

        <!-- Login Screen (Launcher Screen) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ExpenseTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Signup Screen -->
        <activity
            android:name=".ui.auth.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.ExpenseTracker" />

        <!-- Main Screen (Expense List) -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.ExpenseTracker" />

        <!-- Add Expense Screen (Same folder as MainActivity) -->
        <activity
            android:name=".ui.expense.AddExpenseActivity"
            android:exported="false"
            android:theme="@style/Theme.ExpenseTracker" />

        <!-- Edit Expense Screen (Same folder as MainActivity) -->
        <activity
            android:name=".ui.expense.EditExpenseActivity"
            android:exported="false"
            android:theme="@style/Theme.ExpenseTracker" />

    </application>

</manifest>
